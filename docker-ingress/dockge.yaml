# docker-node: iris.haven
# port: 4100

# Service
apiVersion: v1
kind: Service
metadata:
  name: dockge-service
  namespace: docker-ingress
spec:
  ports:
    - port: 80
      targetPort: 5001
---
# Endpoints
apiVersion: v1
kind: Endpoints
metadata:
  name: dockge-service
  namespace: docker-ingress
subsets:
  - addresses:
      - ip: 192.168.15.101
    ports:
      - port: 5001
---
# Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dockge-ingress
  namespace: docker-ingress
spec:
  rules:
    - host: dockge.haven
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dockge-service
                port:
                  number: 80
